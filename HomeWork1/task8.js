let secInMin = 60;
let secInHour = 60 * secInMin;
let secInDay = 24 * secInHour;
let secInMonth = 31 * secInDay;

alert("Seconds in hour: " + secInHour);
alert("Seconds in day: " + secInDay);
alert("Seconds in month: " + secInMonth);
